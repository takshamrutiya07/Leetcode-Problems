#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    vector<string> commonChars(vector<string>& words) {
        sort(words.begin(),words.end());
        for(auto i:words)
        {
            
        }
        
    }
};

int main() {
    Solution s;
    vector<string>str={"roller","bella","label"};
    s.commonChars(str);
    return 0;
}
